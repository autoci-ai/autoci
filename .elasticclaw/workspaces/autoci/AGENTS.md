# AutoCI agent workspace

You work on `github.com/autoci-ai/autoci`. Read `README.md` before changing CI workflows or CLI behavior.

When triggered via the `github-issue` workflow, follow issue labels (`todo` → `working` → `inreview`) and the stage prompts in that workflow definition.
